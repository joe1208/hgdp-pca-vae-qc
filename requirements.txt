# Python dependencies for the analysis code (developed on Python 3.11).
# Install with:  pip install -r requirements.txt
#
# Two dependencies are NOT pip packages and must be provided separately:
#   * PLINK 2 (v2.00a6LM): system binary used by the QC and PCA shell steps.
#     https://www.cog-genomics.org/plink/2.0/
#   * pgenlib: the PLINK 2 Python binding used by genotype_loader.py to read
#     .pgen files. It ships with the PLINK 2 source / 2.0 toolchain rather than
#     as a standalone wheel on all platforms.
#
# torch was used with CUDA 12.4 on the GPU nodes; install the build that matches
# your CUDA/CPU setup (see https://pytorch.org). The VAE step needs a GPU.

torch==2.4.1
scikit-learn==1.3.2
numpy
scipy          # transitive (via scikit-learn); listed for completeness, not imported directly
pandas
matplotlib
